name: Build wtgutil (manual)

on:
  workflow_dispatch:

jobs:
  run-flutter-tag-build:
    uses: ./.github/workflows/dotnet-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: Manual-Build-${{ github.run_id }}